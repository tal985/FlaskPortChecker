# Flask Website + TCP Port Checker
Requires nmap hooked up to the path environment variable. \
This is a test website made with the Flask framework and is used to check if a TCP port is open at a certain IP.